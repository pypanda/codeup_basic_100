#include <stdio.h>

int main(void)
{
	int n, i, j, c = 24;
	int a[10000] = {};
	scanf("%d", &n);
	for (i = 1; i <= n; i++)
	{
		scanf("%d", &a[i]);
	}
	for (i = 1; i <=n ; i ++)
	{
		if (c > a[i])
			c = a[i] ;
	}
	printf("%d", c);
	return 0;
}
