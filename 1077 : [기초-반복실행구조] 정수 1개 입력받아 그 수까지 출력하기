#include <stdio.h>

int main(void)
{
	int x,	t = 0;
	scanf("%d", &x);
	do
	{
		printf("%d\n", t);
		t += 1;
	} while (t < x + 1);
	return 0;
}

/* another method (using for loop)

#include <stdio.h>

int main(void)
{
	int x,	i = 0;
	scanf("%d", &x);
	for (i = 0; i <= x; i++)
	{
		printf("%d\n", i);
	}
	return 0;
}
*/
